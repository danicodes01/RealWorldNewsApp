name: Daily scrape

on:
  schedule:
    # Weekdays only (Mon–Fri) — skipping weekends while the API billing-sync
    # issue is open. Revert the last field to `*` to resume daily runs.
    - cron: '0 11 * * 1-5'   # 07:00 EDT / 06:00 EST — morning
    - cron: '0 15 * * 1-5'   # 11:00 EDT / 10:00 EST — late morning (catches sources that post after 10 UTC)
    - cron: '0 21 * * 1-5'   # 17:00 EDT / 16:00 EST — afternoon refresh
  workflow_dispatch:

concurrency:
  group: scrape-${{ github.ref }}
  cancel-in-progress: false

jobs:
  scrape:
    runs-on: ubuntu-latest
    timeout-minutes: 25
    strategy:
      fail-fast: false
      max-parallel: 2
      matrix:
        source:
          - aljazeera
          - bbc
          - borderlandbeat
          - courthousenews
          - democracynow
          - dropsitenews
          - intercept
          - jacobin
          - npr
    name: scrape-${{ matrix.source }}
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-node@v6
        with:
          node-version: '20'
          cache: 'npm'

      - run: npm ci

      - run: npx playwright install --with-deps chromium

      - name: Run scraper (${{ matrix.source }})
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          INGEST_URL: ${{ secrets.INGEST_URL }}
          INGEST_SECRET: ${{ secrets.INGEST_SECRET }}
          SCRAPE_LIMIT: '15'
        run: npm run scraper:${{ matrix.source }}
